function setup () {
    createCanvas (500, 500);
  button = createButton("more scary babies")
  buttonSound.setVolume(.3);

  buttons = selectAll = ('button');
  console.log (buttons)

  firstButton = select('#firstButton')
  button.mousePressed();
  //console.log(firstButton)

  for 



}

function draw() {
  background (140, 50, 30, 200);
}

function changeBackground() {
  console.log ('button press is working')
  background (0);
}

function mousePressed() {

}
